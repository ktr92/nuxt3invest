
const serviceTableData = {
/*   getTotal(portfolio: IPortfolio, ) {
    return portfolio.positions.reduce((accumulator, item) => accumulator + item., portfolio.depo)
  },
  getTotalChange(portfolio: IPortfolio) {

  } */
}

export default serviceTableData